# Virtual environment
venv/

# Trained model files (large binaries — users train their own)
models/saved_models/

# Database (runtime data, not source)
data/tarpit_logs.db

# NSL-KDD raw data (too large, users download their own)
data/raw/

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# OS
.DS_Store
